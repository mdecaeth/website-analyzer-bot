services:
  - type: web
    name: website-analyzer-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn webapp:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
